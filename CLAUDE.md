# Project: [AAI-XXXX Task Name]

## Старт сессии
1. Прочитать `PROGRESS.md` — там статус, что сделано, следующий шаг
2. Проверить раздел "В работе" — если есть записи старше 4ч, спросить пользователя: завершено / упало / продолжается?
3. Если есть `context/data_schema.md` — прочитать
4. Уточнить у пользователя цель сессии если неясно

## Цель
[Направление исследования или задача — может быть нечёткой на старте]

## Данные
- Source: [откуда]
- Schema: `context/data_schema.md` — создаётся и обновляется по мере изучения данных

## Правила работы
- Векторизованные операции, никаких циклов по большим данным
- Сначала sample, потом full
- После каждой значимой единицы работы — обновить PROGRESS.md и сделать коммит
- Субагенты и параллельные сессии: перед стартом записать задачу в "В работе",
  после завершения — убрать оттуда, добавить в "Сделано", сделать коммит
- Если подход не сработал — записать в PROGRESS.md в раздел Dead ends с причиной
- Перед записью в Results log — проверить: результат согласуется с данными?
  нет утечки? метрика считается корректно?
- При обнаружении проверки которая должна выполняться регулярно — добавить в раздел Oracle

## Oracle
<!-- Пополняется по ходу проекта. Каждая проверка — конкретная, запускаемая. -->
