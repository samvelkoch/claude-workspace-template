# Гигиена вывода

У LLM конечное контекстное окно. Шумный вывод вытесняет полезную информацию.

- Не дампить DataFrame в stdout → `shape=(1M, 47), nulls=0, dtypes ok`
- Не печатать полные массивы или длинные value_counts → агрегаты: top-3, перцентили, counts
- Verbose вывод → `logs/`, не в stdout
- После эксперимента: одна строка с датой, метриками, дельтой к baseline

✓ `[2026-03-25] price_relative: MRR=0.334 (+0.024 к baseline) | quick`
✗ дамп DataFrame на тысячи строк в чат
